# RSQL005

Unresolved interpolation {`{Content}`} detected on line `{Line}`, column `{Column}`

## Solution

You will need to either change how what value is going into the placeholder slot or report the issue on GitHub so that a proper solution can be investigated.

